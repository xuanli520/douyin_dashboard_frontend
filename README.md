
  # Clickable Frontend Prototype

  This is a code bundle for Clickable Frontend Prototype. The original project is available at https://www.figma.com/design/LGaK76dU4vyw4JpT47UBff/Clickable-Frontend-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  